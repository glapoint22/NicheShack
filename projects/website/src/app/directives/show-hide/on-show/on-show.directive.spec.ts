import { OnShowDirective } from './on-show.directive';

describe('OnShowDirective', () => {
  it('should create an instance', () => {
    const directive = new OnShowDirective();
    expect(directive).toBeTruthy();
  });
});
